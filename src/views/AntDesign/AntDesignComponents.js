import React from "react"; //, { useState }
import 'antd/dist/antd.css';

export default function AntDesignComponents() {
  //   const [data, setdata] = useState(null);
  // useEffect(() => {
  // fetch("https://api.nasa.gov/planetary/apod?api_key=DEMO_KEY")
  // .then((res) => res.json())
  // .then((json) => {
  // setdata(json);
  // });
  // }, [data]);
  return (
    <div>
    </div>
  );
}
